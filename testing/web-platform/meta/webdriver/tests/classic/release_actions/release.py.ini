[release.py]
  expected:
    if (display == "wayland") and debug: [OK, TIMEOUT]
    if (os == "linux") and (display == "x11") and debug and fission: [OK, CRASH]
  [test_no_browsing_context]
    expected:
      if os == "win": [PASS, ERROR]
